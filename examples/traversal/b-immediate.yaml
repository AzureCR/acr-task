# In this example, all three commands will run in parallel. '-' stands for immediate execution.
version: v1.0.0

steps:
  - cmd: bash ls
    when: ['-']

  - cmd: bash ls
    when: ['-']

  - cmd: bash ls
    when: ['-']